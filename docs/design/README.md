# design — 검토 중 초안

확정 전 설계 초안을 둔다. 합의가 끝나면 `docs/specs/`로 옮긴다.

| 디렉터리 | 담는 것 |
|---|---|
| `docs/design/` | 검토 중 초안 — 논의 중이라 바뀔 수 있음 |
| `docs/specs/` | 확정 스펙 — 구현이 따르는 기준 |
| `docs/reports/` | 실환경 테스트 결과 |

파일명은 `<기능>-design.md`, 확정 후 `docs/specs/<기능>-spec.md`.
